# 项目总任务队列

本文记录所有已完成项目、未完成项目和封存路线。后续推进时按编号顺序开发，每个可验收版本都需要测试、中文提交并推送到局域网 Forgejo；涉及运行时或部署行为变化时，还需要部署 NAS 并验收。

## 0. 开发流程编号

1. `FLOW-01` 明确本轮要推进的队列编号和验收目标。
2. `FLOW-02` 阅读相关代码、测试和历史文档，避免重复实现。
3. `FLOW-03` 完成代码或文档修改。
4. `FLOW-04` 运行相关测试；风险较高时运行全量测试。
5. `FLOW-05` 如涉及运行时、容器、NAS 行为，部署 NAS 并做接入验收。
6. `FLOW-06` 更新队列、交接文档和当前状态。
7. `FLOW-07` 使用中文提交信息提交。
8. `FLOW-08` 推送到局域网 Forgejo。

## 1. 当前决策

1. `DEC-01` 微信路线暂时封存，不再作为当前开发队列推进。
2. `DEC-02` 已完成的微信通用 Hook、外部消息入口、媒体上传入口和 PC 托盘骨架保留在代码中，不删除、不继续扩展。
3. `DEC-03` 当前主线回到 QQ/NapCat 消息备份、离线可用性、展示体验和生产化完善。

## 2. 已完成队列

1. `DONE-01` QQ/NapCat OneBot 11 反向 WebSocket 接入。
2. `DONE-02` 适配器与机器人身份档案分离，避免更换 bot 后消息串线。
3. `DONE-03` QQ 主视角隔离、全局消息池去重。
4. `DONE-04` CQ 文本消息解析、入库和查询。
5. `DONE-05` CQ 图片、动画表情、语音、视频、文件包/文档解析与本地缓存。
6. `DONE-06` CQ 卡片、链接、小程序 JSON/XML 解析和展示。
7. `DONE-07` 合并转发消息基础解析与前端展开。
8. `DONE-08` 回复消息基础预览。
9. `DONE-09` 图片弹窗预览。
10. `DONE-10` 头像、群名称、群资料、私聊资料和机器人资料缓存。
11. `DONE-11` 媒体回填服务、离线审计和离线修复。
12. `DONE-12` 角色抓取策略：默认抓取、黑名单、白名单、按内容类型控制。
13. `DONE-13` 文件包/文档抓取默认关闭；图片、动画表情、语音不归入文件范畴。
14. `DONE-14` 导出/导入包 checksum、系统签名和媒体嵌入。
15. `DONE-15` 自动备份状态、手动备份、数据库托管 cron/retention 配置。
16. `DONE-16` 审计日志、高风险限流、静态多角色 Token、数据库托管 Token。
17. `DONE-17` 数据库用户、登录态、退出、禁用、密码重置、会话列表、强制下线、Token 轮换。
18. `DONE-18` 轻量迁移注册表、`/api/system/migrations`、Alembic CLI、容器内 `alembic upgrade head`。
19. `DONE-19` FFmpeg 可选转码：内置静态 FFmpeg 镜像可用，NAS `/api/system/runtime` 返回 `ffmpeg_available=true`，WAV 转 MP3 smoke test 通过。
20. `DONE-20` Forgejo SSH 专用 key 已注册，SSH 推送可用。
21. `DONE-21` 微信通用 Hook 入口、外部消息入口、媒体上传入口和 PC 托盘骨架已完成但封存。
22. `DONE-22` QQ 离线审计缺失原因分类、原因汇总、前端缺失说明和修复入口提示。
23. `DONE-23` QQ 媒体回填失败原因细分、结构化统计和 API/前端结果展示。
24. `DONE-24` QQ 合并转发自动后台拉取与媒体缓存加固，连接断开时清理后台任务，避免数据库锁。
25. `DONE-25` QQ 回复消息跳转缺失态：点击引用块找不到原消息时显示明确提示。
26. `DONE-26` QQ 前端审计聊天区空状态压缩，减少无效说明占用空间。

## 3. 未完成开发队列

### 3.1 `Q-01` QQ 离线完整性验收

状态：代码完成，NAS 真实库离线验收通过，进入持续观察。

前置：`DONE-05`、`DONE-11`。

目标：

1. 验证历史消息在断网后仍能完整浏览。
2. 覆盖文本、图片、动画表情、语音、视频、文件包/文档、卡片、合并转发、回复消息。
3. 对缺失资产给出明确原因，而不是只显示文件名或空白。

开发步骤：

1. 梳理 `/api/offline/audit` 对 QQ 消息类型的覆盖。
2. 增加缺失原因分类和测试样本。
3. 更新前端缺失状态展示。
4. 运行相关测试和必要的全量测试。

验收：

1. `/api/offline/audit` 没有高优先级缺失项，或全部有明确不可恢复原因。
2. 前端断网访问已缓存消息时，已入库媒体可预览或播放。
3. 缺失媒体显示可理解的缺失状态和修复入口。

### 3.2 `Q-02` QQ 媒体回填与缺失原因细化

状态：代码完成，NAS 历史索引缺口已修复，进入持续观察。

前置：`Q-01`。

目标：

1. 完善图片、动画表情、语音、视频、文件包/文档的回填逻辑。
2. 区分下载失败、URL 过期、策略跳过、文件类型禁用、源端未提供 URL、本地文件丢失。
3. 回填任务支持批量执行和失败记录。

开发步骤：

1. 扩展媒体回填服务的失败类型。
2. 调整 `failures.log` 结构化记录。
3. 更新前端回填结果统计。
4. 补充服务层和 API 测试。

验收：

1. 手动回填后，能恢复的媒体进入本地缓存。
2. 不能恢复的媒体保留稳定缺失原因。
3. `failures.log` 和前端状态一致。

### 3.3 `Q-03` QQ 合并转发与卡片消息增强

状态：代码完成，NAS 离线审计 `uncached_forwards=0`、`uncached_card_pages=0`，进入持续观察。

前置：`Q-01`、`Q-02`。

目标：

1. 合并转发不用用户点开也尽量缓存子消息。
2. 卡片消息保留原始 JSON/XML、摘要、标题、预览图、跳转 URL。
3. 小卡片可点击打开对应网页。

开发步骤：

1. 检查 NapCat 对合并转发详情的拉取接口和当前实现缺口。
2. 保存合并转发子消息、发送者、时间、媒体引用。
3. 完善卡片消息摘要、预览图、URL 和快照缓存。
4. 增加前端展开、跳转和离线展示测试。

验收：

1. 合并转发详情能在系统内打开并离线查看已缓存内容。
2. 卡片消息在前端可读、可跳转。
3. 离线时仍显示已缓存摘要和快照。

### 3.4 `Q-04` QQ 回复消息跳转体验

状态：代码完成，进入真实样本持续观察。

前置：`Q-01`。

目标：

1. 回复消息展示为类似 QQ 的引用块。
2. 点击引用块可跳转到被回复的原消息。
3. 原消息不存在或未缓存时显示明确状态。

开发步骤：

1. 后端补充回复目标消息查询能力。
2. 前端实现滚动定位、高亮和缺失提示。
3. 覆盖群聊和私聊回复样本。

验收：

1. 群聊和私聊回复都能跳转。
2. 长聊天记录跳转后目标消息有明显定位反馈。
3. 原消息缺失时状态清楚。

### 3.5 `Q-05` QQ 前端审计体验完善

状态：代码完成，进入 UI 现场持续观察。

前置：`Q-01`。

目标：

1. 优化右侧聊天查看区域的信息密度和空状态。
2. 避免无用说明占据上半区。
3. 检查头像、群名、私聊昵称、机器人账号头像显示稳定性。

开发步骤：

1. 检查左侧账号列表、会话列表和消息气泡头像来源。
2. 优化右侧空状态和已选择会话状态。
3. 增加前端静态断言或回归测试。

验收：

1. 选择机器人和会话后聊天区优先展示消息。
2. 左侧账号、会话列表、消息气泡头像稳定显示。

### 3.6 `Q-06` 生产化回归与部署验收

状态：持续项。

前置：每个可验收版本完成后执行。

目标：

1. 每批功能完成后运行相关测试和必要的全量测试。
2. 涉及部署变化时部署 NAS 并做基础验收、离线验收、关键接口验收。
3. 更新交接文档和队列状态。

验收：

1. 中文提交。
2. 推送到 Forgejo。
3. 涉及部署变化时，NAS 验收结果写入交接文档。

### 3.7 `AUDIT-P1` 审计 P1 整改队列

来源：`AUDIT_P2_FIXED.md`。已按 `AUDIT_P1_FIXED.md` 跳过 `P1-01`、`P1-04`、`P1-08`。

状态：待开发。

目标：

1. 修复剩余 P1 安全、性能和数据一致性风险。
2. 每项独立或小批量提交，补充对应回归测试。
3. 修改涉及运行时行为时纳入 `Q-06` 做 NAS 验收。

开发顺序：

1. `AUDIT-P1-02` 媒体下载缺少文件类型白名单验证。
2. `AUDIT-P1-03` 数据库连接池未配置，高并发可能耗尽连接。
3. `AUDIT-P1-05` 密码哈希使用不安全 SHA256，需要 bcrypt/passlib 与旧哈希兼容迁移。
4. `AUDIT-P1-06` 动态表名缺少白名单验证，存在未来 SQL 注入风险。
5. `AUDIT-P1-07` 媒体回填逐条提交可能造成数据库死锁，需要批量提交和并发保护。

验收：

1. 对应单元测试或回归测试通过。
2. 全量高风险路径测试通过。
3. 涉及依赖、容器或数据库行为变化时完成 NAS 验收。

### 3.8 `AUDIT-P2` 审计 P2 整改队列

来源：`AUDIT_P2_FIXED.md`。

状态：待开发。

目标：

1. 提升生产部署安全性、容错能力、可观测性和前端稳定性。
2. 优先处理低风险、收益明确的后端加固，再处理前端体验和部署瘦身。

开发顺序：

1. `AUDIT-P2-01` 环境变量验证不完整。
2. `AUDIT-P2-03` 缺少 API 请求大小限制。
3. `AUDIT-P2-07` FFmpeg 转码未设置超时。
4. `AUDIT-P2-08` 合并转发递归深度未限制。
5. `AUDIT-P2-02` 时区处理不一致。
6. `AUDIT-P2-04` 备份文件名可预测，存在时序攻击风险。
7. `AUDIT-P2-05` 前端缺少输入验证和 XSS 防护。
8. `AUDIT-P2-06` 日志记录不足，难以排查生产问题。
9. `AUDIT-P2-12` 缺少健康检查依赖项检测。
10. `AUDIT-P2-13` 审计日志未脱敏敏感信息。
11. `AUDIT-P2-09` 数据库迁移缺少回滚机制。
12. `AUDIT-P2-10` 导出包未压缩，大量消息时文件过大。
13. `AUDIT-P2-11` 前端路由缺失，刷新后状态丢失。
14. `AUDIT-P2-14` Docker 镜像体积较大。
15. `AUDIT-P2-15` 前端错误处理不友好。

验收：

1. 每项有明确失败场景或验收样例。
2. 前端改动需在本地浏览器验证主要交互。
3. 部署行为变化需更新文档并纳入 NAS 验收。

### 3.9 `AUDIT-P3` 审计 P3 优化队列

来源：`AUDIT_P2_FIXED.md`。

状态：待开发。

目标：

1. 完善文档、监控、工程化和长期可维护性。
2. 不阻塞当前 QQ 主线，但按顺序逐项收口。

开发顺序：

1. `AUDIT-P3-01` 缺少 API 文档示例和说明。
2. `AUDIT-P3-02` 缺少性能监控和指标导出。
3. `AUDIT-P3-03` 缺少 WebSocket 心跳机制。
4. `AUDIT-P3-05` 缺少数据库索引优化。
5. `AUDIT-P3-06` 测试覆盖率不足。
6. `AUDIT-P3-07` 缺少 Docker Compose 健康检查依赖。
7. `AUDIT-P3-08` 前端代码未压缩和混淆。
8. `AUDIT-P3-09` 缺少速率限制说明和监控。
9. `AUDIT-P3-04` 前端未做离线缓存。
10. `AUDIT-P3-10` 缺少数据备份恢复演练文档。
11. `AUDIT-P3-11` 缺少 CI/CD 配置。
12. `AUDIT-P3-12` 缺少贡献指南和开发者文档。

验收：

1. 文档项可按文档完成一次实际操作或审阅。
2. 工程化项在本地或 Forgejo 环境可执行。
3. 监控和指标项有可访问的验证入口。

## 4. 封存路线

### 4.1 `HOLD-01` 微信 PC / WeChatFerry 路线

状态：封存。

封存原因：

1. 新版微信 4.x 与当前 `wcferry 39.5.2.0` 不兼容。
2. 已安装并校验旧版微信 `3.9.12.51`，但本机 HVCI/内存完整性会阻止 WCF 注入链路。
3. 使用独立旧电脑或虚拟机会占用同一个个人微信号的 PC 登录，不符合当前使用约束。
4. PadLocal/iPad 协议和数据库读取方案均存在账号风险、外部依赖或媒体完整性不足问题。

保留内容：

1. `POST /api/wechat/events`
2. `POST /api/receive_external_msg`
3. `POST /api/external/media`
4. `POST /api/wechat/media`
5. `wechat_tray_adapter/`
6. 微信样本回放测试

恢复条件：

1. 用户明确重新开启微信路线。
2. 或出现稳定、低风险、不占用当前 PC 登录、可后台缓存媒体的可行方案。
