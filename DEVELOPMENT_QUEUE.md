# 开发队列

本队列承接当前审计出的未完成项目。后续每完成一个可验收版本，按以下流程推进：

1. 更新代码与测试。
2. 运行相关测试和必要的全量测试。
3. 使用中文提交信息提交。
4. 推送到局域网 Forgejo 仓库。
5. 对需要部署的功能，部署到 NAS 并执行接入验收。

## P0 当前决策

微信路线暂时封存，开发重心回到 QQ/NapCat 消息备份、离线可用性和审计体验完善。

已完成的微信通用入口、外部消息入口、媒体上传入口和 PC 托盘骨架保留为封存能力，不删除、不继续扩展。

## P1 已完成并进入生产样本持续验收

### 1. QQ 离线完整性验收

状态：代码完成，NAS 真实库验收通过。

目标：

- 全量扫描已入库 QQ 消息和媒体资产。
- 断网后仍可查看历史文本、图片、动画表情、语音、视频、文件包/文档、卡片、合并转发和回复消息。
- 对不可恢复资产输出明确缺失原因。

开发项：

- 梳理 `/api/offline/audit` 的 QQ 检查项。
- 扩展媒体缺失分类。
- 前端展示缺失原因与修复入口。
- 补充测试样本。

验收：

- 高优先级缺失项清零，或全部有明确不可恢复原因。
- 已缓存媒体断网可打开。
- 2026-07-06 NAS 离线审计扫描 9269 条消息、3659 个媒体资产、300 个头像，缺失项 0。

### 2. QQ 媒体回填增强

状态：代码完成，NAS 历史索引缺口已修复。

目标：

- 提升图片、动画表情、语音、视频、文件包/文档的可恢复率。
- 让回填过程可观测、可重试、可审计。

开发项：

- 区分下载失败、URL 过期、策略跳过、文件类型禁用、源端未提供 URL、本地缓存丢失。
- 批量回填支持更清晰的结果统计。
- `failures.log` 保留结构化失败原因。
- 前端回填按钮和状态提示继续打磨。

验收：

- 能恢复的媒体进入本地缓存。
- 不能恢复的媒体不再被误判为系统 bug。
- 2026-07-06 NAS 离线修复补齐 12 个历史媒体索引，复查后 `offline_ready=true`。

### 3. QQ 合并转发与卡片消息增强

状态：代码完成，进入真实样本持续观察。

目标：

- 合并转发消息尽量自动缓存子消息，不依赖用户手动点开。
- 卡片消息可读、可点、可离线审计。

开发项：

- 检查 NapCat 对合并转发详情的可拉取接口和当前实现缺口。
- 保存合并转发的子消息、发送者、时间、媒体引用。
- 卡片消息保留原始 JSON/XML、标题、摘要、预览图、URL。
- 可选网页快照缓存。

验收：

- 合并转发详情可在本系统内打开。
- 卡片点击直接打开对应网页。
- 离线时仍显示已缓存摘要和快照。
- 当前 NAS 离线审计 `uncached_forwards=0`、`uncached_card_pages=0`。

### 4. QQ 回复消息跳转体验

状态：代码完成，进入真实样本持续观察。

目标：

- 回复消息显示为类似 QQ 的引用块。
- 点击引用块跳转到被回复消息。

开发项：

- 后端补充回复目标消息查询。
- 前端实现定位、高亮和滚动。
- 原消息缺失时显示明确状态。

验收：

- 群聊、私聊回复均可跳转。
- 长聊天记录中定位准确。
- 原消息未缓存或已被清理时，前端显示明确缺失态。

### 5. QQ 前端审计体验完善

状态：代码完成，进入 UI 现场持续观察。

目标：

- 提高聊天查看区域的信息密度。
- 修复头像、群名、私聊昵称、机器人账号头像显示回归。
- 减少无用空状态占位。

开发项：

- 检查左侧账号列表、会话列表和消息气泡头像来源。
- 优化右侧空状态和已选择会话状态。
- 增加前端回归测试或静态断言。

验收：

- 选择机器人和会话后聊天区优先展示消息。
- 头像与群名稳定显示。
- 聊天区空状态已压缩为简短提示，避免占据上半区。

## P2 已完成生产化增强

- FFmpeg 与媒体转码流水线：已完成并在 NAS 验收通过。
- 导出包系统识别码签名：已完成。
- 自动备份前端配置入口：已完成。
- 生产权限、限流与操作审计：已完成。
- 数据库迁移体系：已完成。
- Forgejo SSH 鉴权：已完成。
- 角色抓取黑白名单与内容项策略：已完成。

## P3 运维和文档收口

状态：持续项。

目标：

- 每次版本推进后更新 `PROJECT_HANDOFF_READ_ME_FIRST.md`、`TASK_QUEUE.md` 和本文档。
- 记录最新提交、测试数量、NAS 状态和剩余队列。
- 不写入 token、密码或私钥。

验收：

- 文档与当前主线一致。
- 后续接手无需翻完整聊天记录。

## 封存项

### 微信 PC / WeChatFerry / PadLocal / 数据库读取

状态：封存。

原因：

- WCF 需要兼容旧版微信和可注入环境，本机 HVCI/内存完整性阻断注入。
- 关闭主力机内存完整性风险较高。
- 独立采集机或虚拟机会占用同一个微信号的 PC 登录。
- PadLocal/iPad 协议依赖外部 Token，存在账号风控。
- 数据库读取不能保证未点开的图片、视频、文件完整缓存。

恢复条件：

- 用户明确重新开启。
- 或出现更稳定、低风险、不占用当前 PC 登录的方案。
